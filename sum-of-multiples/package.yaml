name: sum-of-multiples
version: 1.5.0.10

dependencies:
  - base

library:
  exposed-modules: SumOfMultiples
  source-dirs: src
  ghc-options: -Wall
  dependencies:
  - containers       # List here the packages you


tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - sum-of-multiples
      - hspec
